apiVersion: v1
kind: Pod
metadata:
  name: fast-api-pod
spec:
  containers:
  - name: fastapi
    image: krotym2/fastapi:latest
    ports:
    - containerPort: 8000
    command: ["uvicorn", "main:app", "--reload"]